# Easy-Fine-Tunnin 项目详细介绍

## 📋 项目概述

**Easy-Fine-Tunnin** 是一个专为大型语言模型（LLM）微调数据集创建而设计的全功能应用程序。它提供了一个完整的workflow，从文档处理到数据集导出，支持多种文件格式和AI模型提供商。

- **项目名称**: Easy-Fine-Tunnin
- **当前版本**: v1.6.1
- **许可证**: AGPL 3.0
- **作者**: ConardLi
- **GitHub**: https://github.com/ConardLi/easy-dataset
- **论文**: [arXiv:2507.04009](https://arxiv.org/abs/2507.04009v1)

## 🎯 核心功能

### 1. 智能文档处理
- **支持格式**: PDF、Markdown、DOCX、EPUB、TXT
- **智能分割**: 多种文本分割算法，支持自定义分隔符
- **目录提取**: 自动提取文档目录结构
- **批量处理**: 支持批量上传和处理多个文件

### 2. 问题生成系统
- **自动生成**: 基于文本块自动生成相关问题
- **模板支持**: 支持问题模板，可自定义问题格式
- **标签管理**: 智能构建全局领域标签树
- **GA对生成**: 支持Genre-Audience对生成，用于多角度问题生成

### 3. 答案生成系统
- **多模型支持**: 兼容所有遵循OpenAI格式的LLM API
- **思维链生成**: 支持Chain of Thought (COT) 生成
- **批量生成**: 支持批量生成答案
- **自定义提示词**: 支持项目级别的自定义提示词

### 4. 数据集管理
- **多格式导出**: 支持Alpaca、ShareGPT、multilingual-thinking等格式
- **文件格式**: 支持JSON、JSONL导出
- **数据清洗**: 内置数据清洗和优化功能
- **质量评估**: AI驱动的数据集质量评估

### 5. 多轮对话生成
- **对话场景**: 支持教学、咨询、讨论等多种场景
- **角色设定**: 支持自定义对话角色
- **轮数控制**: 可设置最大对话轮数
- **格式兼容**: 完全兼容ShareGPT格式

### 6. 图像数据集支持
- **图像标注**: 支持图像上传和标注
- **视觉问答**: 支持基于图像的问题和答案生成
- **多答案类型**: 支持文本、标签、自定义格式答案

### 7. 数据蒸馏
- **自动蒸馏**: 自动从现有数据集中提取高质量数据
- **标签蒸馏**: 智能提取和优化标签结构

## 🏗️ 技术架构

### 技术栈

#### 前端技术
- **框架**: Next.js 14 (App Router)
- **UI库**: Material-UI (MUI) v5
- **状态管理**: Jotai
- **动画**: Framer Motion
- **国际化**: i18next + react-i18next
- **Markdown渲染**: react-markdown

#### 后端技术
- **运行时**: Node.js
- **数据库**: SQLite (通过Prisma ORM)
- **文件处理**: 
  - PDF: @opendocsg/pdf2md, pdf2md-js
  - DOCX: mammoth
  - EPUB: 自定义解析器
- **AI集成**: 
  - @ai-sdk/openai
  - ollama-ai-provider
  - zhipu-ai-provider
  - @openrouter/ai-sdk-provider
  - @ai-sdk/openai-compatible

#### 桌面应用
- **框架**: Electron
- **自动更新**: electron-updater
- **打包工具**: electron-builder

#### 开发工具
- **包管理**: npm/pnpm
- **代码规范**: Prettier, Commitlint
- **Git钩子**: Husky, lint-staged

### 项目结构

```
easy-dataset/
├── app/                          # Next.js App Router
│   ├── api/                      # API路由
│   │   ├── check-update/         # 更新检查
│   │   ├── llm/                  # LLM相关API
│   │   ├── projects/             # 项目相关API (73个文件)
│   │   └── update/               # 更新API
│   ├── dataset-square/           # 数据集广场页面
│   ├── projects/                 # 项目相关页面
│   │   └── [projectId]/          # 项目详情页
│   │       ├── datasets/         # 数据集管理
│   │       ├── distill/          # 数据蒸馏
│   │       ├── image-datasets/   # 图像数据集
│   │       ├── images/           # 图像管理
│   │       ├── multi-turn/       # 多轮对话
│   │       ├── playground/       # 模型测试
│   │       ├── questions/        # 问题管理
│   │       ├── settings/         # 项目设置
│   │       ├── tasks/            # 任务管理
│   │       └── text-split/       # 文本分割
│   ├── globals.css               # 全局样式
│   ├── layout.js                 # 根布局
│   └── page.js                   # 首页
│
├── components/                    # React组件
│   ├── common/                   # 通用组件
│   ├── conversations/            # 对话相关组件
│   ├── dataset-square/           # 数据集广场组件
│   ├── datasets/                 # 数据集组件
│   ├── distill/                  # 数据蒸馏组件
│   ├── export/                   # 导出组件
│   ├── home/                     # 首页组件
│   ├── mga/                      # GA对管理组件
│   ├── Navbar/                   # 导航栏组件
│   ├── playground/               # 测试组件
│   ├── questions/                # 问题组件
│   ├── settings/                 # 设置组件
│   ├── tasks/                    # 任务组件
│   └── text-split/               # 文本分割组件
│
├── constant/                      # 常量定义
│   ├── index.js                  # 通用常量
│   ├── model.js                  # 模型配置
│   ├── setting.js                # 设置常量
│   └── sites.json                 # 站点配置
│
├── electron/                     # Electron桌面应用
│   ├── main.js                   # 主进程
│   ├── preload.js                # 预加载脚本
│   └── modules/                  # 模块
│       ├── cache.js              # 缓存管理
│       ├── database.js           # 数据库初始化
│       ├── db-updater.js         # 数据库更新
│       ├── ipc-handlers.js       # IPC处理
│       ├── logger.js             # 日志系统
│       ├── menu.js               # 菜单
│       ├── server.js             # 服务器启动
│       ├── updater.js            # 更新器
│       └── window-manager.js      # 窗口管理
│
├── hooks/                        # React Hooks
│   ├── useDebounce.js            # 防抖Hook
│   ├── useFileProcessingStatus.js # 文件处理状态
│   ├── useGenerateDataset.js     # 数据集生成
│   ├── useModelPlayground.js     # 模型测试
│   ├── useSnackbar.js            # 消息提示
│   └── useTaskSettings.js        # 任务设置
│
├── lib/                          # 核心库
│   ├── api/                      # API客户端
│   │   ├── chunk.js              # 文本块API
│   │   ├── file.js               # 文件API
│   │   ├── index.js              # API入口
│   │   └── task.js               # 任务API
│   │
│   ├── db/                       # 数据库访问层 (20个文件)
│   │   ├── base.js               # 基础工具
│   │   ├── chunks.js             # 文本块
│   │   ├── custom-prompts.js     # 自定义提示词
│   │   ├── dataset-conversations.js # 对话数据集
│   │   ├── datasets.js           # 数据集
│   │   ├── files.js              # 文件
│   │   ├── ga-pairs.js           # GA对
│   │   ├── images.js             # 图像
│   │   ├── imageDatasets.js      # 图像数据集
│   │   ├── model-config.js       # 模型配置
│   │   ├── projects.js           # 项目
│   │   ├── questions.js          # 问题
│   │   ├── questionTemplates.js  # 问题模板
│   │   ├── tags.js               # 标签
│   │   └── upload-files.js       # 上传文件
│   │
│   ├── file/                     # 文件处理
│   │   ├── file-process/         # 文件处理核心
│   │   │   ├── check-file.js     # 文件检查
│   │   │   ├── get-content.js    # 内容提取
│   │   │   ├── index.js          # 入口
│   │   │   ├── epub/             # EPUB处理
│   │   │   └── pdf/              # PDF处理
│   │   │       ├── default.js    # 默认处理
│   │   │       ├── mineru.js     # MinerU处理
│   │   │       ├── vision.js     # 视觉模型处理
│   │   │       └── prompt/       # PDF提示词
│   │   ├── split-markdown/       # Markdown分割
│   │   │   ├── core/             # 核心逻辑
│   │   │   │   ├── parser.js     # 解析器
│   │   │   │   ├── splitter.js   # 分割器
│   │   │   │   ├── summary.js    # 摘要生成
│   │   │   │   └── toc.js        # 目录提取
│   │   │   ├── output/          # 输出处理
│   │   │   └── utils/           # 工具函数
│   │   └── text-splitter.js      # 文本分割器
│   │
│   ├── llm/                      # LLM集成
│   │   ├── common/              # 通用工具
│   │   │   ├── prompt-loader.js  # 提示词加载器
│   │   │   ├── question-template.js # 问题模板
│   │   │   └── util.js          # 工具函数
│   │   ├── core/                # 核心模块
│   │   │   ├── index.js         # LLM客户端
│   │   │   └── providers/       # 提供商实现
│   │   │       ├── base.js      # 基础类
│   │   │       ├── ollama.js    # Ollama
│   │   │       ├── openai.js    # OpenAI
│   │   │       ├── openrouter.js # OpenRouter
│   │   │       ├── zhipu.js     # 智谱AI
│   │   │       └── alibailian.js # 阿里云百炼
│   │   └── prompts/             # 提示词模板 (16个文件)
│   │       ├── answer.js        # 答案生成
│   │       ├── dataClean.js     # 数据清洗
│   │       ├── datasetEvaluation.js # 数据集评估
│   │       ├── distillQuestions.js # 问题蒸馏
│   │       ├── distillTags.js   # 标签蒸馏
│   │       ├── enhancedAnswer.js # 增强答案
│   │       ├── ga-generation.js # GA对生成
│   │       ├── imageAnswer.js   # 图像答案
│   │       ├── imageQuestion.js # 图像问题
│   │       ├── label.js         # 标签生成
│   │       ├── multiTurnConversation.js # 多轮对话
│   │       ├── newAnswer.js     # 新答案
│   │       ├── optimizeCot.js  # COT优化
│   │       └── question.js      # 问题生成
│   │
│   ├── services/                # 业务逻辑层 (21个文件)
│   │   ├── clean.js             # 数据清洗
│   │   ├── datasets/            # 数据集服务
│   │   │   ├── evaluation.js    # 评估服务
│   │   │   └── index.js        # 数据集服务
│   │   ├── ga/                  # GA对服务
│   │   │   ├── ga-generation.js # GA对生成
│   │   │   └── ga-pairs.js     # GA对管理
│   │   ├── images/              # 图像服务
│   │   ├── models.js            # 模型服务
│   │   ├── multi-turn/          # 多轮对话服务
│   │   ├── questions/           # 问题服务
│   │   │   ├── index.js        # 问题生成
│   │   │   └── template.js     # 问题模板
│   │   └── tasks/               # 任务服务
│   │       ├── answer-generation.js      # 答案生成任务
│   │       ├── data-cleaning.js          # 数据清洗任务
│   │       ├── data-distillation.js      # 数据蒸馏任务
│   │       ├── dataset-evaluation.js     # 数据集评估任务
│   │       ├── file-processing.js        # 文件处理任务
│   │       ├── image-dataset-generation.js # 图像数据集生成
│   │       ├── image-question-generation.js # 图像问题生成
│   │       ├── index.js                  # 任务入口
│   │       ├── multi-turn-generation.js   # 多轮对话生成
│   │       ├── question-generation.js    # 问题生成任务
│   │       └── recovery.js                # 任务恢复
│   │
│   ├── i18n.js                  # 国际化配置
│   ├── store.js                 # 状态存储
│   └── util/                    # 工具函数
│       ├── async.js             # 异步工具
│       ├── domain-tree.js       # 领域树
│       ├── file.js              # 文件工具
│       ├── image.js             # 图像工具
│       ├── logger.js            # 日志工具
│       ├── processInParallel.js # 并行处理
│       └── request.js            # 请求工具
│
├── locales/                      # 国际化资源
│   ├── en/                      # 英文
│   ├── tr/                      # 土耳其语
│   └── zh-CN/                   # 简体中文
│
├── prisma/                       # Prisma配置
│   ├── schema.prisma            # 数据库模式
│   ├── sql.json                 # SQL模板
│   └── generate-template.js     # 模板生成
│
├── public/                       # 静态资源
│   └── imgs/                    # 图片资源
│
├── styles/                       # 样式文件
│   ├── globals.css              # 全局样式
│   ├── home.js                  # 首页样式
│   └── playground.js            # 测试页样式
│
├── docker-compose.yml           # Docker Compose配置
├── Dockerfile                   # Docker构建文件
├── docker-entrypoint.sh         # Docker入口脚本
├── next.config.js               # Next.js配置
├── package.json                 # 项目配置
└── README.md                    # 项目说明
```

## 📊 数据模型

### 核心数据表

#### Projects (项目)
- 项目基本信息（名称、描述）
- 自定义提示词配置（全局、问题、答案、标签等）
- 关联所有项目数据

#### UploadFiles (上传文件)
- 文件元信息（名称、扩展名、路径、大小、MD5）
- 关联到项目

#### Chunks (文本块)
- 分割后的文本块
- 包含内容、摘要、大小等信息
- 关联到上传文件

#### Questions (问题)
- 生成的问题
- 关联到文本块或图像
- 支持问题模板
- 支持GA对关联

#### Datasets (数据集)
- 问题-答案对
- 包含答案类型（文本/标签/自定义格式）
- 支持COT（思维链）
- 支持评分和AI评估

#### DatasetConversations (对话数据集)
- 多轮对话数据
- 支持角色设定和场景配置
- 完全兼容ShareGPT格式

#### Tags (标签)
- 树形结构的标签系统
- 支持父子关系
- 用于问题分类

#### ModelConfig (模型配置)
- 项目级别的模型配置
- 支持多个模型提供商
- 包含API密钥和参数配置

#### Task (任务)
- 异步任务管理
- 支持多种任务类型：
  - `file-processing`: 文件处理
  - `question-generation`: 问题生成
  - `answer-generation`: 答案生成
  - `data-distillation`: 数据蒸馏
  - `data-cleaning`: 数据清洗
  - `dataset-evaluation`: 数据集评估
  - `multi-turn-generation`: 多轮对话生成
  - `image-question-generation`: 图像问题生成
  - `image-dataset-generation`: 图像数据集生成

#### GaPairs (GA对)
- Genre-Audience对
- 用于多角度问题生成
- 每个文件可生成5个GA对

#### Images (图像)
- 图像元信息
- 支持图像标注

#### ImageDatasets (图像数据集)
- 基于图像的数据集
- 支持多种答案类型

#### QuestionTemplates (问题模板)
- 可复用的问题模板
- 支持文本和图像源
- 支持多种答案类型

## 🔄 核心工作流

### 1. 文档处理流程

```
文件上传 → 文件类型检测 → 内容提取 → 文本分割 → 目录提取 → 存储到数据库
```

**支持的文件格式**:
- PDF: 使用pdf2md或MinerU进行转换
- Markdown: 直接解析和分割
- DOCX: 使用mammoth转换
- EPUB: 自定义解析器
- TXT: 直接处理

### 2. 问题生成流程

```
选择文本块 → 加载提示词模板 → 调用LLM API → 解析问题 → 提取标签 → 存储到数据库
```

**生成方式**:
- 基于文本块自动生成
- 使用问题模板生成
- 基于GA对生成多角度问题

### 3. 答案生成流程

```
选择问题 → 加载答案提示词 → 调用LLM API → 提取答案和COT → 存储到数据集
```

**答案类型**:
- 文本答案
- 标签答案（多选）
- 自定义格式答案（JSON）

### 4. 多轮对话生成流程

```
选择文本块 → 设置场景和角色 → 生成初始问题 → 多轮对话生成 → 格式化为ShareGPT格式
```

### 5. 数据导出流程

```
选择数据集 → 选择导出格式 → 应用自定义提示词 → 格式转换 → 文件下载
```

**导出格式**:
- Alpaca格式
- ShareGPT格式
- Multilingual-thinking格式

## 🚀 部署方式

### 1. 本地开发

```bash
# 安装依赖
npm install

# 初始化数据库
npm run db:push

# 启动开发服务器
npm run dev
# 访问 http://localhost:1717
```

### 2. 生产构建

```bash
# 构建Next.js应用
npm run build

# 启动生产服务器
npm run start
```

### 3. Electron桌面应用

```bash
# 开发模式
npm run electron-dev

# 构建安装包
npm run electron-build        # 所有平台
npm run electron-build-mac    # macOS
npm run electron-build-win    # Windows
npm run electron-build-linux  # Linux
```

### 4. Docker部署

```bash
# 使用官方镜像
docker-compose up -d

# 或构建本地镜像
docker build -t easy-dataset .
docker run -d -p 1717:1717 \
  -v ./local-db:/app/local-db \
  -v ./prisma:/app/prisma \
  easy-dataset
```

## 🔌 支持的AI模型提供商

### 已集成的提供商

1. **Ollama** - 本地模型运行
2. **OpenAI** - GPT系列模型
3. **硅基流动 (SiliconFlow)** - 国内模型服务
4. **DeepSeek** - DeepSeek系列模型
5. **302.AI** - 多模型聚合服务
6. **智谱AI (ZhiPu)** - GLM系列模型
7. **火山引擎 (Doubao)** - 豆包模型
8. **Groq** - 高速推理服务
9. **Grok** - xAI模型
10. **OpenRouter** - 多模型聚合平台
11. **阿里云百炼 (Alibailian)** - 通义千问模型

### 模型配置

每个项目可以配置多个模型，支持：
- 自定义API端点
- API密钥管理
- 温度、最大Token等参数配置
- 不同任务使用不同模型

## 🌍 国际化支持

### 支持的语言

- **简体中文 (zh-CN)**: 完整支持
- **English (en)**: 完整支持
- **Türkçe (tr)**: 完整支持

### 国际化范围

- UI界面文本
- 所有AI提示词
- 错误消息
- 帮助文档

## 📈 任务系统

### 任务类型

1. **文件处理任务** (`file-processing`)
   - 批量处理上传的文件
   - 自动分割和提取目录

2. **问题生成任务** (`question-generation`)
   - 批量生成问题
   - 支持并发处理

3. **答案生成任务** (`answer-generation`)
   - 批量生成答案
   - 支持流式输出

4. **数据清洗任务** (`data-cleaning`)
   - 自动清洗数据集
   - 优化答案质量

5. **数据蒸馏任务** (`data-distillation`)
   - 从现有数据集中提取高质量数据
   - 标签优化

6. **数据集评估任务** (`dataset-evaluation`)
   - AI驱动的质量评估
   - 自动评分

7. **多轮对话生成任务** (`multi-turn-generation`)
   - 批量生成多轮对话
   - 支持多种场景

8. **图像问题生成任务** (`image-question-generation`)
   - 基于图像生成问题

9. **图像数据集生成任务** (`image-dataset-generation`)
   - 基于图像生成完整数据集

### 任务状态

- `0`: 处理中
- `1`: 已完成
- `2`: 失败
- `3`: 已中断

### 任务恢复

系统支持任务恢复机制，可以自动恢复中断的任务。

## 🎨 用户界面特性

### 主要页面

1. **首页**
   - 项目列表展示
   - 项目创建
   - 数据统计

2. **文本分割页**
   - 文件上传
   - 文本块查看和编辑
   - 目录树展示
   - 批量编辑

3. **问题管理页**
   - 问题列表/树形视图
   - 问题编辑
   - 标签管理
   - 问题模板管理

4. **数据集页**
   - 数据集列表
   - 答案编辑
   - 数据筛选
   - 批量操作

5. **多轮对话页**
   - 对话列表
   - 对话详情查看
   - 场景配置

6. **图像数据集页**
   - 图像上传
   - 图像标注
   - 数据集生成

7. **数据蒸馏页**
   - 自动蒸馏
   - 标签蒸馏
   - 进度展示

8. **任务管理页**
   - 任务列表
   - 任务详情
   - 任务筛选

9. **项目设置页**
   - 基础设置
   - 模型配置
   - 提示词自定义
   - 任务设置

10. **模型测试页 (Playground)**
    - 实时测试模型
    - 流式输出展示

### UI特性

- **响应式设计**: 支持桌面和移动端
- **深色模式**: 支持主题切换
- **动画效果**: 使用Framer Motion提供流畅动画
- **实时更新**: 任务进度实时更新
- **批量操作**: 支持批量编辑和删除

## 🔧 开发指南

### 环境要求

- Node.js >= 18
- npm 或 pnpm
- SQLite (通过Prisma自动管理)

### 开发命令

```bash
# 开发模式
npm run dev

# 数据库管理
npm run db:studio    # 打开Prisma Studio
npm run db:push      # 推送数据库变更

# 代码格式化
npm run prettier

# 构建
npm run build
```

### 代码规范

- 使用ES6+语法
- 模块化开发
- 异步操作使用async/await
- 错误处理使用try/catch
- 注释使用JSDoc格式

### 添加新功能

1. **添加新的AI模型提供商**
   - 在 `lib/llm/core/providers/` 创建新provider
   - 实现基础接口 (chat, chatStream)
   - 在 `lib/llm/core/index.js` 中注册
   - 更新 `constant/model.js`

2. **添加新的文件格式支持**
   - 在 `lib/file/file-process/` 创建格式处理器
   - 实现内容提取逻辑
   - 更新文件类型检测

3. **添加新的导出格式**
   - 在 `components/export/` 创建导出组件
   - 实现格式转换逻辑
   - 更新导出对话框

## 📚 相关资源

### 文档

- **项目文档**: https://docs.easy-dataset.com/ed/en
- **演示视频**: https://www.bilibili.com/video/BV1y8QpYGE57/
- **论文**: https://arxiv.org/abs/2507.04009v1

### 社区实践

- [Easy-Fine-Tunnin × LLaMA Factory](https://buaa-act.feishu.cn/wiki/GVzlwYcRFiR8OLkHbL6cQpYin7g)
- [Easy-Fine-Tunnin实用指南](https://www.bilibili.com/video/BV1MRMnz1EGW)
- [关键功能更新解读](https://www.bilibili.com/video/BV1fyJhzHEb7/)

## 🤝 贡献指南

1. Fork 仓库
2. 创建功能分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'Add some amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 创建 Pull Request (提交到 DEV 分支)

## 📝 许可证

本项目采用 AGPL 3.0 许可证 - 详见 [LICENSE](LICENSE) 文件

## 🙏 致谢

感谢所有为这个项目做出贡献的开发者和用户！

---

**最后更新**: 2025年1月
**维护者**: ConardLi
**项目状态**: 积极维护中

